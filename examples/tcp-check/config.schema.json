{
  "type": "object",
  "properties": {
    "host": {"type": "string"},
    "port": {"type": "integer", "minimum": 1, "maximum": 65535},
    "timeout_ms": {"type": "integer", "minimum": 1},
    "send": {"type": "string"},
    "expect_bytes": {"type": "integer", "minimum": 1}
  },
  "required": ["host", "port"]
}
